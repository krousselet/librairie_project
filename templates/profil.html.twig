{% extends 'base.html.twig' %}

{% block title %}Profil utilisateur{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('style/profil.css') }}">
{% endblock %}

{% block body %}
    {% block header %}
        {{ parent() }}
    {% endblock header %}
    <div class="user-profile">
    <div class="title">User name: <div>{{ app.user.username }}</div></div>
    <div class="title">First name: <div>{{ app.user.firstname }}</div></div>
    <div class="title">Last name: <div>{{ app.user.lastname }}</div></div>
    <div class="title">Email: <div>{{ app.user.email }}</div></div>
    <div class="title">Address: <div>{{ app.user.address }}</div></div>

    <a href="#">Changer mon mot de passe</a>
    </div>

{% endblock body %}
